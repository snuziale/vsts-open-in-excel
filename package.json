{
  "devDependencies": {
    "rimraf": "^2.6.2",
    "tfx-cli": "^0.3.49",
    "typescript": "^2.5.3"
  },
  "name": "vsts-open-in-excel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p .",
    "postbuild": "npm run package",
    "package": "tfx extension create --rev-version --manifests vss-extension.json --overrides-file configs/release.json",
    "package-dev": "tfx extension create --rev-version --manifests vss-extension.json --overrides-file configs/dev.json",
    "gallery-publish": "tfx extension publish --service-url https://marketplace.visualstudio.com --manifests vss-extension.json --overrides-file configs/release.json",
    "gallery-publish-dev": "tfx extension publish --service-url https://marketplace.visualstudio.com --manifests vss-extension.json --overrides-file configs/dev.json",
    "clean": "rimraf ./build && rimraf ./*.vsix"
  },
  "dependencies": {
    "@types/jquery": "^2.0.48",
    "@types/q": "^1.0.6",
    "vss-web-extension-sdk": "^4.125.2"
  }
}
